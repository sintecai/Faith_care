Fix my existing Replit app (Node/Express + TypeScript running with tsx server/index.ts) where Twilio Verify OTP is failing with error "Authenticate" and the admin route shows "Cannot GET /admin".

Context:
- I already added these Replit Secrets (exact names):
  TWILIO_ACCOUNT_SID (starts with AC...)
  TWILIO_API_KEY (starts with SK...)
  TWILIO_API_SECRET (32 chars)
  TWILIO_VERIFY_SERVICE_SID (starts with VA...)
- The app currently logs:
  "Failed to send verification code: Authenticate"
  "Twilio Verify error: Authenticate"
- /admin currently returns "Cannot GET /admin"

Goal:
1) Make Twilio Verify SMS OTP work (real SMS to phone) using the existing secrets.
2) Fix admin routing so /admin loads (or redirects to /admin/login).

Tasks:
A) Twilio integration fixes (must-do)
- Ensure the Twilio SDK is installed (npm i twilio) and imported correctly in TypeScript.
- Create a single Twilio client module, e.g. src/services/twilio.ts, with EXACT initialization using API Key + Secret + Account SID:
    import twilio from "twilio";
    export const twilioClient = twilio(
      process.env.TWILIO_API_KEY as string,
      process.env.TWILIO_API_SECRET as string,
      { accountSid: process.env.TWILIO_ACCOUNT_SID as string }
    );
    export const VERIFY_SID = process.env.TWILIO_VERIFY_SERVICE_SID as string;
- Ensure no code uses TWILIO_AUTH_TOKEN (remove any such usage).
- Ensure env var names used in code match the Replit Secrets exactly.

B) Add a diagnostic endpoint (to isolate the "Authenticate" error)
- Add GET /debug/twilio that:
  1) Logs ONLY safe prefixes/lengths (do NOT print full secrets):
     - Account SID prefix (first 2 chars)
     - API Key prefix (first 2 chars)
     - API Secret length
     - Verify SID prefix (first 2 chars)
  2) Calls Twilio Verify API to send a test OTP to a phone number provided via query param:
     GET /debug/twilio?to=+4477...
     Use twilioClient.verify.v2.services(VERIFY_SID).verifications.create({to, channel:"sms"})
  3) Returns JSON with ok:true and verification sid if success; otherwise ok:false and the Twilio error message + status code.
- This endpoint is temporary and must not expose secrets.

C) Fix the actual OTP flow
- Implement/repair:
  POST /auth/send-otp
    - Validate phone starts with "+"
    - Check member exists in DB (if DB exists). If not available, still send OTP for now.
    - Call Twilio Verify to send SMS OTP using VERIFY_SID
    - Return {ok:true} on success
  POST /auth/verify-otp
    - Verify code using Twilio Verify verificationChecks
    - On approved, set session and redirect to /home
- Remove any old OTP code that stores OTP in DB.

D) Fix admin route error
- Ensure /admin is handled:
  - If admin router exists at /admin/login, then add GET /admin that redirects to /admin/login.
  - If admin router is not mounted, mount it using app.use("/admin", adminRouter).
- Confirm /admin returns a page (or redirect) without "Cannot GET /admin".

E) Finish with clear run & test instructions in README
- How to test Twilio: visit /debug/twilio?to=+<number>
- How to test login OTP
- How to access admin: /admin

Important constraints:
- Do NOT ask me for secrets; use env vars already set.
- Do NOT print secrets in logs.
- Use parameterized DB queries where applicable.
- Keep UI unchanged; focus on fixing Twilio auth + admin route.

Deliverable:
- A working build where /debug/twilio successfully sends OTP SMS and /admin works.
