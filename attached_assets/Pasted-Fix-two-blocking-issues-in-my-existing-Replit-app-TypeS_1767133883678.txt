Fix two blocking issues in my existing Replit app (TypeScript + Express + server-rendered UI):

ISSUE 1: Twilio Verify SMS still failing with "Authenticate" (code 20003)
ISSUE 2: Admin login page shows masked/disabled Sign In button and cannot submit the form

--------------------------------
CONTEXT
--------------------------------
- App runs with: NODE_ENV=development tsx server/index.ts
- Replit Secrets already added:
  TWILIO_ACCOUNT_SID (AC...)
  TWILIO_API_KEY (SK...)
  TWILIO_API_SECRET (32 chars)
  TWILIO_VERIFY_SERVICE_SID (VA...)
  TWILIO_AUTH_TOKEN (added for fallback testing)
- /debug/twilio endpoint exists and shows credentials are loaded
- Error persists: "Twilio Verify error: Authenticate"
- /admin now redirects to /admin/login
- Admin login page loads but Sign In button is masked / disabled / unclickable

--------------------------------
TASK A — FIX TWILIO VERIFY (UP TO SMS WORKING)
--------------------------------
1) Ensure Twilio client is initialised in ONLY ONE place:
   - Create or update src/services/twilio.ts
   - Remove any duplicate twilio(...) initialisations elsewhere

2) Add a safe auth fallback mechanism:
   - Support TWO auth modes:
     a) API Key mode:
        twilio(API_KEY, API_SECRET, { accountSid: ACCOUNT_SID })
     b) Auth Token mode:
        twilio(ACCOUNT_SID, AUTH_TOKEN)
   - Read a boolean env flag USE_TWILIO_AUTH_TOKEN (default false)
   - If true, use Auth Token mode
   - If false, use API Key mode

3) Improve /debug/twilio:
   - Accept ?to=+E164
   - Send OTP using Twilio Verify
   - Return detailed Twilio error info in JSON:
     { ok, status, code, message, moreInfo }
   - Log only safe prefixes and lengths (never full secrets)

4) Ensure /auth/send-otp and /auth/verify-otp:
   - Use the SAME Twilio client
   - Use the SAME Verify Service SID
   - Do NOT store OTP in database
   - Validate phone starts with "+"

5) After fix:
   - /debug/twilio?to=+<number> must successfully send SMS
   - No more "Authenticate" errors

--------------------------------
TASK B — FIX ADMIN LOGIN UI (BUTTON NOT CLICKABLE)
--------------------------------
1) Inspect the admin login page HTML/CSS/JS:
   - Identify why the Sign In button is masked, disabled, or unclickable
   - Common causes to check:
     - button has disabled attribute
     - z-index overlay blocking clicks
     - opacity + pointer-events:none
     - form inside a disabled fieldset
     - missing <form method="POST" action="...">

2) Fix the admin login form so:
   - Email + password fields are editable
   - Sign In button is clickable
   - Form submits correctly to POST /admin/login (or correct route)

3) Ensure:
   - Button type="submit"
   - No JS validation blocking submission unintentionally
   - No CSS overlay or modal blocking interaction

4) Keep UI design unchanged visually — only fix interaction.

--------------------------------
DELIVERABLES
--------------------------------
- Twilio Verify SMS works end-to-end (tested via /debug/twilio)
- Admin login Sign In button works and submits form
- /admin/login is fully usable
- No secrets logged or exposed
- Add brief comments explaining fixes

--------------------------------
IMPORTANT
--------------------------------
- Do NOT ask me for secrets
- Use only existing env vars
- Do NOT redesign pages
- Focus only on fixing SMS auth and admin login usability
